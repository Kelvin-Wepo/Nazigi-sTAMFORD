# System Architecture & Flow Diagrams

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         NAZIGI SMS SERVICE                       │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┐                                    ┌──────────────┐
│              │                                    │              │
│  Passengers  │◄───────── SMS Messages ──────────►│ AfricasTalk  │
│              │           (Shortcode 20880)       │              │
└──────────────┘                                    └──────┬───────┘
                                                           │
                                                           │ Webhook
                                                           │ Callback
                                                           ▼
                                             ┌─────────────────────┐
                                             │   Flask Backend     │
                                             │                     │
┌──────────────┐    HTTPS/REST API          │  • SMS Router       │
│              │◄──────────────────────────►│  • Auth Handler     │
│  Conductor   │   (Basic Auth)             │  • Business Logic   │
│ Web Dashboard│                            │  • SMS Service      │
│              │                            └──────────┬──────────┘
└──────────────┘                                       │
                                                       │ SQLAlchemy
                                                       │ ORM
                                                       ▼
                                             ┌─────────────────────┐
                                             │   PostgreSQL DB     │
                                             │                     │
                                             │  • Passengers       │
                                             │  • Messages         │
                                             │  • Responses        │
                                             │  • SMS Logs         │
                                             └─────────────────────┘
```

---

## Data Flow: Passenger Opt-In

```
┌─────────────┐
│  Passenger  │
└──────┬──────┘
       │
       │ 1. Sends "STAMFORD" to 20880
       ▼
┌─────────────────┐
│ AfricasTalking  │
└──────┬──────────┘
       │
       │ 2. Webhook POST /sms/callback
       ▼
┌──────────────────────┐
│  Flask App           │
│  sms_routes.py       │
└──────┬───────────────┘
       │
       │ 3. Check if passenger exists
       │    If not, create new record
       ▼
┌──────────────────────┐
│  PostgreSQL          │
│  INSERT passengers   │
│  opted_in = false    │
└──────┬───────────────┘
       │
       │ 4. Send opt-in prompt
       ▼
┌──────────────────────┐
│  sms_service.py      │
│  send_sms()          │
└──────┬───────────────┘
       │
       │ 5. Send via AT API
       ▼
┌─────────────────┐
│ AfricasTalking  │
└──────┬──────────┘
       │
       │ 6. Delivers SMS
       ▼
┌─────────────┐
│  Passenger  │
│  Receives:  │
│  "Welcome!  │
│   Reply YES │
│   or NO"    │
└─────────────┘
```

---

## Data Flow: Conductor Sends Message

```
┌──────────────┐
│  Conductor   │
│  (Browser)   │
└──────┬───────┘
       │
       │ 1. Login + Type message
       │    POST /conductor/send-message
       ▼
┌──────────────────────┐
│  Flask App           │
│  conductor_routes.py │
└──────┬───────────────┘
       │
       │ 2. Authenticate (Basic Auth)
       │ 3. Query opted-in passengers
       ▼
┌──────────────────────┐
│  PostgreSQL          │
│  SELECT passengers   │
│  WHERE opted_in=true │
└──────┬───────────────┘
       │
       │ 4. Get phone numbers
       │    [+254712..., +254713...]
       ▼
┌──────────────────────┐
│  sms_service.py      │
│  send_bulk_sms()     │
└──────┬───────────────┘
       │
       │ 5. Add stop list to message
       │    Send to AT API
       ▼
┌─────────────────┐
│ AfricasTalking  │
│ Bulk SMS API    │
└──────┬──────────┘
       │
       │ 6. Delivers to all
       ▼
┌─────────────┐  ┌─────────────┐  ┌─────────────┐
│ Passenger 1 │  │ Passenger 2 │  │ Passenger N │
│ Receives:   │  │ Receives:   │  │ Receives:   │
│ "Bus at CBD │  │ "Bus at CBD │  │ "Bus at CBD │
│  1. Ngara   │  │  1. Ngara   │  │  1. Ngara   │
│  2. Allsops │  │  2. Allsops │  │  2. Allsops │
│  ..."       │  │  ..."       │  │  ..."       │
└─────────────┘  └─────────────┘  └─────────────┘
```

---

## Data Flow: Passenger Selects Stop

```
┌─────────────┐
│  Passenger  │
└──────┬──────┘
       │
       │ 1. Reply "5" or "Zimmerman"
       ▼
┌─────────────────┐
│ AfricasTalking  │
└──────┬──────────┘
       │
       │ 2. Webhook POST /sms/callback
       ▼
┌──────────────────────┐
│  Flask App           │
│  sms_routes.py       │
└──────┬───────────────┘
       │
       │ 3. Parse response
       │    "5" → Check BUS_STOPS[4]
       │    → "Zimmerman"
       ▼
┌──────────────────────┐
│  PostgreSQL          │
│  INSERT              │
│  passenger_responses │
│  selected_stop =     │
│  "Zimmerman"         │
└──────┬───────────────┘
       │
       │ 4. Send confirmation
       ▼
┌──────────────────────┐
│  sms_service.py      │
│  send_sms()          │
└──────┬───────────────┘
       │
       │ 5. Send via AT API
       ▼
┌─────────────────┐
│ AfricasTalking  │
└──────┬──────────┘
       │
       │ 6. Delivers SMS
       ▼
┌─────────────┐
│  Passenger  │
│  Receives:  │
│  "✅ You'll │
│   be picked │
│   at        │
│   Zimmerman"│
└─────────────┘
```

---

## Database Relationships

```
┌────────────────────┐
│    passengers      │
│────────────────────│
│ id (PK)           │◄────┐
│ phone_number      │     │
│ opted_in          │     │
│ created_at        │     │
│ updated_at        │     │
└────────────────────┘     │
                           │ passenger_id (FK)
                           │
┌────────────────────┐     │
│ conductor_messages │     │
│────────────────────│     │
│ id (PK)           │◄────┼──────┐
│ message_text      │     │      │
│ sent_at           │     │      │
│ recipients_count  │     │      │ message_id (FK)
└────────────────────┘     │      │
                           │      │
                    ┌──────┴──────┴────┐
                    │ passenger_        │
                    │ responses         │
                    │───────────────────│
                    │ id (PK)          │
                    │ passenger_id (FK)│
                    │ message_id (FK)  │
                    │ response_text    │
                    │ selected_stop    │
                    │ responded_at     │
                    └──────────────────┘

┌────────────────────┐
│    sms_logs        │
│────────────────────│
│ id (PK)           │
│ phone_number      │  (Independent logging)
│ message           │
│ direction         │
│ status            │
│ created_at        │
└────────────────────┘
```

---

## Web Dashboard Flow

```
┌──────────────┐
│  Conductor   │
│  Browser     │
└──────┬───────┘
       │
       │ 1. Navigate to http://server:5000
       ▼
┌──────────────────────┐
│  Flask App           │
│  GET /               │
│  → conductor.html    │
└──────┬───────────────┘
       │
       │ 2. Login form shown
       ▼
┌──────────────┐
│  Conductor   │
│  Enters:     │
│  username    │
│  password    │
└──────┬───────┘
       │
       │ 3. JavaScript sends credentials
       │    GET /conductor/dashboard
       │    Authorization: Basic xxx
       ▼
┌──────────────────────┐
│  Flask App           │
│  @requires_auth      │
│  Check credentials   │
└──────┬───────────────┘
       │
       │ 4. If valid, return stats
       ▼
┌──────────────────────┐
│  PostgreSQL          │
│  COUNT passengers    │
│  COUNT messages      │
│  COUNT responses     │
└──────┬───────────────┘
       │
       │ 5. Return JSON
       ▼
┌──────────────┐
│  Browser     │
│  Display:    │
│  • Stats     │
│  • Send form │
│  • Responses │
└──────────────┘
```

---

## API Authentication Flow

```
Client Request:
┌─────────────────────────────────────┐
│ POST /conductor/send-message        │
│ Authorization: Basic YWRtaW46cGFzcw==│
│ Content-Type: application/json      │
│                                     │
│ {"message": "Bus leaving CBD..."}   │
└─────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────┐
│ Flask @requires_auth Decorator      │
└─────────────────────────────────────┘
       │
       ├─── Extract Authorization header
       │
       ├─── Decode Base64
       │    "admin:password"
       │
       ├─── Compare with config
       │    CONDUCTOR_USERNAME
       │    CONDUCTOR_PASSWORD
       │
       ├─── If match: Continue
       │    If not: 401 Unauthorized
       │
       ▼
┌─────────────────────────────────────┐
│ Process Request                     │
│ • Query passengers                  │
│ • Send SMS                          │
│ • Log to database                   │
│ • Return response                   │
└─────────────────────────────────────┘
```

---

## SMS Message Lifecycle

```
Stage 1: CREATION
┌──────────────┐
│  Conductor   │
│  composes    │
│  message     │
└──────┬───────┘
       │
       ▼
┌──────────────────────┐
│  conductor_messages  │
│  INSERT new record   │
└──────────────────────┘

Stage 2: SENDING
       │
       ▼
┌──────────────────────┐
│  sms_service.py      │
│  send_bulk_sms()     │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│  sms_logs            │
│  INSERT (outgoing)   │
│  for each recipient  │
└──────────────────────┘
       │
       ▼
┌─────────────────┐
│ AfricasTalking  │
│ API             │
└─────────────────┘

Stage 3: DELIVERY
       │
       ▼
┌─────────────┐
│ Passengers  │
│ receive SMS │
└─────────────┘

Stage 4: RESPONSE
       │
       ▼
┌─────────────────┐
│ Passenger       │
│ replies with    │
│ stop number     │
└──────┬──────────┘
       │
       ▼
┌──────────────────────┐
│  passenger_responses │
│  INSERT new record   │
│  link to message_id  │
└──────────────────────┘
       │
       ▼
┌──────────────────────┐
│  sms_logs            │
│  INSERT (incoming)   │
└──────────────────────┘
```

---

## Complete System Integration

```
                    NAZIGI SMS SERVICE
                    ══════════════════

External Systems:
┌─────────────────────────────────────────────────┐
│                                                 │
│  AfricasTalking Platform                       │
│  • SMS Gateway                                 │
│  • Webhook Callbacks                           │
│  • Delivery Reports                            │
│                                                 │
└────────────┬────────────────────────────────────┘
             │
             │ HTTPS/Webhooks
             │
┌────────────┴────────────────────────────────────┐
│                                                 │
│  Nazigi Flask Application                      │
│  ┌──────────────────────────────────────────┐  │
│  │  Web Layer                               │  │
│  │  • conductor.html (Dashboard)            │  │
│  │  • Static assets                         │  │
│  └──────────────────────────────────────────┘  │
│                                                 │
│  ┌──────────────────────────────────────────┐  │
│  │  API Layer                               │  │
│  │  • sms_routes.py (Callbacks)             │  │
│  │  • conductor_routes.py (Admin API)       │  │
│  └──────────────────────────────────────────┘  │
│                                                 │
│  ┌──────────────────────────────────────────┐  │
│  │  Business Logic Layer                    │  │
│  │  • sms_service.py (SMS Operations)       │  │
│  │  • models.py (Data Models)               │  │
│  │  • config.py (Configuration)             │  │
│  └──────────────────────────────────────────┘  │
│                                                 │
└────────────┬────────────────────────────────────┘
             │
             │ SQLAlchemy ORM
             │
┌────────────┴────────────────────────────────────┐
│                                                 │
│  PostgreSQL Database                           │
│  • passengers (User data)                      │
│  • conductor_messages (Broadcasts)             │
│  • passenger_responses (Replies)               │
│  • sms_logs (Audit trail)                      │
│                                                 │
└─────────────────────────────────────────────────┘

Users:
┌─────────────┐          ┌──────────────┐
│  Passengers │          │  Conductors  │
│             │          │              │
│  SMS Client │          │  Web Browser │
└─────────────┘          └──────────────┘
```

---

## Deployment Architecture (Production)

```
Internet
   │
   │ HTTPS (443)
   ▼
┌─────────────────┐
│  Nginx          │  Reverse Proxy + SSL
│  • SSL Cert     │  • Load Balancing
│  • Static Files │  • Rate Limiting
└────────┬────────┘
         │
         │ HTTP (5000)
         ▼
┌─────────────────┐
│  Gunicorn       │  WSGI Server
│  (4 workers)    │  • Process Management
└────────┬────────┘
         │
         │
         ▼
┌─────────────────┐
│  Flask App      │  Application
│  Nazigi SMS     │  • Business Logic
└────────┬────────┘
         │
         ├──────────────────┬──────────────────┐
         │                  │                  │
         ▼                  ▼                  ▼
┌────────────────┐  ┌────────────────┐  ┌────────────────┐
│  PostgreSQL    │  │ AfricasTalking │  │  File System   │
│  Database      │  │  API           │  │  • Logs        │
│  (localhost)   │  │  (External)    │  │  • Static      │
└────────────────┘  └────────────────┘  └────────────────┘

System Services:
┌─────────────────────────────────────────┐
│  systemd                                │
│  • nazigi-sms.service (Auto-restart)    │
│  • postgresql.service                   │
│  • nginx.service                        │
└─────────────────────────────────────────┘
```

---

## Security Layers

```
┌──────────────────────────────────────────────┐
│  Network Layer                               │
│  • Firewall (ufw)                            │
│  • Allow: 80, 443                            │
│  • Deny: Direct 5000 access                  │
└──────────────────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────────────┐
│  Transport Layer                             │
│  • SSL/TLS (Let's Encrypt)                   │
│  • HTTPS Only                                │
└──────────────────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────────────┐
│  Application Layer                           │
│  • HTTP Basic Auth (Conductor)               │
│  • Environment Variables (.env)              │
│  • No hardcoded credentials                  │
└──────────────────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────────────┐
│  Data Layer                                  │
│  • SQLAlchemy ORM (SQL injection protection) │
│  • Parameterized queries                     │
│  • Input validation                          │
└──────────────────────────────────────────────┘
```

---

## Monitoring & Logging

```
┌────────────────────────────────────────┐
│  Application Logs                      │
│  • Flask logger                        │
│  • journalctl (systemd)                │
│  • /var/log/nazigi-sms/                │
└────────────┬───────────────────────────┘
             │
             ▼
┌────────────────────────────────────────┐
│  Database Logs                         │
│  • sms_logs table                      │
│  • All incoming/outgoing SMS           │
│  • Delivery status                     │
└────────────┬───────────────────────────┘
             │
             ▼
┌────────────────────────────────────────┐
│  Web Server Logs                       │
│  • Nginx access log                    │
│  • Nginx error log                     │
│  • /var/log/nginx/                     │
└────────────────────────────────────────┘

Monitoring Tools:
• tail -f /var/log/nazigi-sms/app.log
• sudo journalctl -u nazigi-sms -f
• psql nazigi_sms -c "SELECT * FROM sms_logs..."
```

---

This architecture provides a robust, scalable, and maintainable SMS service! 🚀
